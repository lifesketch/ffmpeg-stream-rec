# Копируй в .env и правь при необходимости
#
# Docker Compose:
#   — порт на хосте (внутри контейнера всегда 5000): HOST_PORT=8080
#   — записи и БД лежат на хосте в ./docker-data/ (см. docker-compose.yml), не в recordings/
#     Строки RECORDINGS_ROOT / STATE_DB_PATH / LOG_DIR ниже — для локального python -m recorder.
# HOST_PORT=8080
SECRET_KEY=замени-на-случайную-строку
RECORDINGS_ROOT=recordings
STATE_DB_PATH=data/state.sqlite3
LOG_DIR=logs
MAX_INDEPENDENT_SESSIONS=2
MAX_CONTINUATIONS=5
# Базовое имя MP4 в форме и при пустом поле: steam1 → steam1_001.mp4, …
DEFAULT_RECORDING_BASENAME=steam1
FFMPEG_BIN=ffmpeg
FFPROBE_BIN=ffprobe
# 1 — stderr FFmpeg в PIPE (поле ffmpeg_stderr_excerpt в /record/status). На macOS/Jupyter
# при «0 KB» и пустом MP4 оставьте закомментированным: по умолчанию stderr в DEVNULL.
# RECORDING_FFMPEG_STDERR_PIPE=1
# Если задан — нужен заголовок Authorization: Bearer <токен>
# RECORDING_AUTH_TOKEN=
# При SIGTERM/Ctrl+C и выходе процесса — мягкая остановка FFmpeg (0/false — отключить)
# RECORDING_SHUTDOWN_HOOKS=1
# Макс. секунд ожидания завершения сессий перед выходом процесса
# RECORDING_SHUTDOWN_WAIT_SEC=90
